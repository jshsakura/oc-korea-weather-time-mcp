[project]
name = "oc-korea-weather-time-mcp"
version = "0.1.0"
description = "기상청 날씨 + 시간대 조회 MCP 서버 (Korean weather & timezone MCP server)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "jshsakura" }]
keywords = ["mcp", "model-context-protocol", "weather", "korea", "kma", "기상청", "timezone"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Atmospheric Science",
    "Natural Language :: Korean",
]
dependencies = [
    "mcp>=2.0.0",
    "httpx>=0.27",
]

[project.urls]
Homepage = "https://github.com/jshsakura/oc-korea-weather-time-mcp"
Repository = "https://github.com/jshsakura/oc-korea-weather-time-mcp"
Issues = "https://github.com/jshsakura/oc-korea-weather-time-mcp/issues"

[project.scripts]
oc-korea-weather-time-mcp = "oc_korea_weather_time_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/oc_korea_weather_time_mcp"]
